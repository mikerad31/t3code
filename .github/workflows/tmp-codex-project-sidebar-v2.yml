name: Validate Codex project sidebar v2

on:
  push:
    branches:
      - validate/codex-project-sidebar-v2

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4
        with:
          version: 11.10.0

      - uses: actions/setup-node@v4
        with:
          node-version: 24.13.1
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Apply guarded sidebar controls patch
        run: |
          python .github/scripts/tmp-codex-project-sidebar-v2-logic.py
          python .github/scripts/tmp-codex-project-sidebar-v2-ui.py

      - name: Format product files
        run: |
          pnpm exec vp fmt \
            apps/web/src/components/Sidebar.logic.ts \
            apps/web/src/components/Sidebar.logic.test.ts \
            apps/web/src/components/Sidebar.tsx

      - name: Assert exact product surface
        run: |
          cat > /tmp/expected-files.txt <<'EOF'
          apps/web/src/components/Sidebar.logic.test.ts
          apps/web/src/components/Sidebar.logic.ts
          apps/web/src/components/Sidebar.tsx
          EOF
          sort -o /tmp/expected-files.txt /tmp/expected-files.txt
          git diff --name-only 4d73921e688bdc75d260b6bd8be1024669769663 \
            | grep -v '^.github/scripts/tmp-codex-project-sidebar-v2-' \
            | grep -v '^.github/workflows/tmp-codex-project-sidebar-v2.yml$' \
            | sort > /tmp/actual-files.txt
          diff -u /tmp/expected-files.txt /tmp/actual-files.txt

      - name: Repository conformance
        run: pnpm exec vp check

      - name: Sidebar logic regressions
        run: pnpm --filter @t3tools/web exec vp test run src/components/Sidebar.logic.test.ts

      - name: Full monorepo typecheck
        run: pnpm typecheck

      - name: Final diff sanity
        run: |
          git diff --check
          test -z "$(git diff --name-only --diff-filter=U)"
          git diff --name-only 4d73921e688bdc75d260b6bd8be1024669769663 \
            | grep -v '^.github/scripts/tmp-codex-project-sidebar-v2-' \
            | grep -v '^.github/workflows/tmp-codex-project-sidebar-v2.yml$' \
            | sort > /tmp/actual-files-after.txt
          diff -u /tmp/expected-files.txt /tmp/actual-files-after.txt

      - name: Stage validated product blobs
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p /tmp/sidebar-v2-blobs
          : > /tmp/sidebar-v2-blobs/manifest.txt
          while IFS= read -r file; do
            base64 -w0 "$file" > /tmp/blob-content.b64
            jq -n --rawfile content /tmp/blob-content.b64 \
              '{content: $content, encoding: "base64"}' > /tmp/blob-request.json
            sha="$(gh api "repos/${GITHUB_REPOSITORY}/git/blobs" \
              --method POST --input /tmp/blob-request.json --jq .sha)"
            printf '%s %s\n' "$sha" "$file" | tee -a /tmp/sidebar-v2-blobs/manifest.txt
          done < /tmp/expected-files.txt

      - uses: actions/upload-artifact@v4
        with:
          name: codex-project-sidebar-v2-blobs
          path: /tmp/sidebar-v2-blobs/manifest.txt
          retention-days: 1
